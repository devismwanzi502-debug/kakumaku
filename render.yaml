services:
  - type: web
    name: network-monitor-web
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node api-server/server.js
    envVars:
      - key: NODE_ENV
        value: production